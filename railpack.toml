[build]
providers = ["python"]

[env]
DJANGO_SETTINGS_MODULE = "realestate_project.settings"
PYTHONUNBUFFERED = "1"

[start]
cmd = "gunicorn realestate_project.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --worker-class sync"
