{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "gunicorn realestate_project.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --worker-class sync"
  }
}
